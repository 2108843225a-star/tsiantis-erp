// ΤΣΙΑΝΤΗΣ ERP — Prisma schema
//
// Στόχος: πλήρες, multi-user, real-time-capable domain model για τη
// WINDOWS-MARKT. PostgreSQL (βλ. docs/ARCHITECTURE.md για το γιατί).
//
// Concurrency: κάθε πίνακας που επεξεργάζονται πολλοί χρήστες/το Claude
// ταυτόχρονα έχει `version Int @default(1)` (optimistic locking — κάθε
// write ελέγχει WHERE version = X, ελαχιστοποιεί lost updates χωρίς
// database-wide locks) + `updatedAt` timestamp. Transactions χρησιμοποιούνται
// στο service layer (όχι εδώ) γύρω από πολυβηματικές αλλαγές.
//
// ΔΕΝ τρέχει ακόμα σε πραγματική βάση σε αυτό το sandbox (χρειάζεται
// DATABASE_URL προς πραγματικό Postgres) — βλ. STATUS.md για το ένα
// σημείο που χρειάζεται από τον Φίλιππο.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ---------- Users / Roles / Permissions ----------

enum Role {
  OWNER_ADMIN
  SECRETARY
  TECHNICIAN
}

model User {
  id                 String    @id @default(cuid())
  email              String    @unique
  name               String
  role               Role
  active             Boolean   @default(true)
  passwordHash       String    // scrypt "salt:hash" — βλ. src/lib/auth/password.ts
  permissionOverrides Json?    // { grant: string[], revoke: string[] } — βλ. src/lib/rbac/roles.ts
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt

  quotesCreated      Quote[]        @relation("QuoteCreatedBy")
  ordersCreated      Order[]        @relation("OrderCreatedBy")
  paymentsRecorded   Payment[]      @relation("PaymentRecordedBy")
  serviceAssigned    ServiceTicket[] @relation("ServiceAssignedTo")
  auditEntries       AuditLog[]     @relation("AuditActorUser")
}

// ---------- Πελάτες / Έργα ----------

model Customer {
  id        String   @id @default(cuid())
  firstName String
  lastName  String
  phone     String?
  email     String?
  location  String?
  notes     String?
  version   Int      @default(1)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  projects  Project[]

  @@index([lastName])
  @@index([phone])
}

enum ProjectStatus {
  ΝΕΟ
  ΜΕΤΡΗΣΗ
  ΚΟΣΤΟΛΟΓΗΣΗ
  ΠΡΟΣΦΟΡΑ
  ΑΠΟΔΟΧΗ
  ΠΑΡΑΓΓΕΛΙΑ
  ΠΑΡΑΓΩΓΗ
  ΤΟΠΟΘΕΤΗΣΗ
  ΚΛΕΙΣΜΕΝΟ
}

model Project {
  id             String        @id @default(cuid())
  code           String        @unique // π.χ. "2026-0142"
  customerId     String
  customer       Customer      @relation(fields: [customerId], references: [id])
  location       String?
  status         ProjectStatus @default(ΝΕΟ)
  agreedAmount   Decimal?      @db.Decimal(10, 2)
  installationDate DateTime?
  notes          String?
  version        Int           @default(1)
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt

  openings       Opening[]
  quotes         Quote[]
  orders         Order[]
  payments       Payment[]
  documents      ProjectDocument[]
  serviceTickets ServiceTicket[]

  @@index([status])
  @@index([code])
}

// ---------- Κουφώματα / Μετρήσεις / Κοπές ----------

enum WindowSystem {
  EUROPA_850
  EUROPA_8500
  PVC
  OTHER_UNCONFIRMED // σειρά χωρίς CONFIRMED κανόνα — αποθηκεύεται, ΔΕΝ υπολογίζεται
}

model Opening {
  id            String       @id @default(cuid())
  projectId     String
  project       Project      @relation(fields: [projectId], references: [id])
  unitCode      String       // Κ01, Κ02, ...
  system        WindowSystem
  typology      String       // ΜΟΝΟΦΥΛΛΟ / ΔΙΦΥΛΛΟ / ΣΤΑΘΕΡΟ / ΠΑΝΤΖΟΥΡΙ / ...
  openingWidth  Int          // mm
  openingHeight Int          // mm
  color         String?
  glassSpec     String?
  hasRoller     Boolean      @default(false)
  hasScreen     Boolean      @default(false)
  hasShutter    Boolean      @default(false)
  notes         String?
  version       Int          @default(1)
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt

  cuttingResults CuttingResult[]

  @@unique([projectId, unitCode])
}

model CuttingResult {
  id           String   @id @default(cuid())
  openingId    String
  opening      Opening  @relation(fields: [openingId], references: [id])
  resultJson   Json     // πλήρες CuttingResult από το cut engine (βλ. src/lib/cutting/*)
  sourceRuleIds String[] // RULE_IDs που χρησιμοποιήθηκαν — audit trail
  qcStatus     String   @default("PENDING") // PENDING / CONFIRMED / DISCREPANCY
  qcResultJson Json?    // αποτέλεσμα ανεξάρτητου δεύτερου ελέγχου
  calculatedBy String   // user id ή "CLAUDE"
  createdAt    DateTime @default(now())
}

// ---------- Business Rules & Pricing Variables ----------

enum RuleStatus {
  VERIFIED
  USER_VERIFIED
  PENDING
  SUPERSEDED
}

model BusinessRule {
  id                 String     @id @default(cuid())
  ruleId             String     // π.χ. "EUROPA_850_MONOFYLLO_CUT" — σταθερό αναγνωριστικό ανά family
  category           String
  system             String
  typology           String
  variables          String[]
  formula            String
  source             String
  status             RuleStatus
  version            Int
  validFrom          DateTime
  validTo            DateTime?
  verificationState  String
  updatedBy          String
  updatedAt          DateTime   @updatedAt
  createdAt          DateTime   @default(now())

  @@unique([ruleId, version])
  @@index([ruleId, status])
}

model PricingVariable {
  id         String   @id @default(cuid())
  key        String   // π.χ. "ROLLER_CATALOG_DISCOUNT_PERCENT"
  value      Decimal  @db.Decimal(10, 4)
  validFrom  DateTime
  validTo    DateTime?
  updatedBy  String
  updatedAt  DateTime @updatedAt
  createdAt  DateTime @default(now())

  @@index([key, validFrom])
}

// ---------- Τιμοκατάλογοι / Ιστορικά snapshot ----------

model PriceCatalogEntry {
  id            String   @id @default(cuid())
  sku           String
  description   String
  catalogPrice  Decimal  @db.Decimal(10, 2)
  discountAllowed Boolean @default(true)
  supplier      String?
  source        String
  validFrom     DateTime
  validTo       DateTime?
  updatedAt     DateTime @updatedAt
  createdAt     DateTime @default(now())

  @@unique([sku, validFrom])
  @@index([sku])
}

// Αμετάβλητο snapshot ό,τι χρησιμοποιήθηκε σε ΜΙΑ συγκεκριμένη κοστολόγηση —
// ποτέ δεν ξαναγράφεται όταν αλλάζει ο τιμοκατάλογος/η έκπτωση αργότερα.
model PriceSnapshot {
  id                String   @id @default(cuid())
  quoteId           String?
  quote             Quote?   @relation(fields: [quoteId], references: [id])
  sku               String
  catalogPrice      Decimal  @db.Decimal(10, 2)
  discountAllowed   Boolean
  discountPercent   Decimal  @db.Decimal(5, 2)
  discountAmount    Decimal  @db.Decimal(10, 2)
  netPurchasePrice  Decimal  @db.Decimal(10, 2)
  priceDate         DateTime
  priceSource       String
  createdAt         DateTime @default(now())
}

// ---------- Προσφορές / Παραγγελίες / Πληρωμές ----------

enum QuoteStatus {
  DRAFT
  SENT
  ACCEPTED
  REJECTED
}

model Quote {
  id            String      @id @default(cuid())
  projectId     String
  project       Project     @relation(fields: [projectId], references: [id])
  status        QuoteStatus @default(DRAFT)
  customerTotal Decimal     @db.Decimal(10, 2)
  internalCost  Decimal     @db.Decimal(10, 2)
  marginPercent Decimal     @db.Decimal(5, 2)
  vatPercent    Decimal     @db.Decimal(5, 2)
  createdById   String
  createdBy     User        @relation("QuoteCreatedBy", fields: [createdById], references: [id])
  version       Int         @default(1)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  priceSnapshots PriceSnapshot[]
}

enum OrderCategory {
  ΑΛΟΥΜΙΝΙΑ
  ΤΖΑΜΙΑ
  ΕΞΑΡΤΗΜΑΤΑ
  ΣΙΤΕΣ
  ΡΟΛΑ
  ΑΝΑΛΩΣΙΜΑ
}

model Order {
  id           String        @id @default(cuid())
  projectId    String
  project      Project       @relation(fields: [projectId], references: [id])
  category     OrderCategory
  supplier     String?
  itemsJson    Json          //3���s�s�3�3�s��3�3�s�s�s���s���s����]\���[��Y�][
��Q��B�ܙX]Y�RY��[�ܙX]Y�H\�\��[][ۊ�ܙ\�ܙX]Y�H��Y[Έ�ܙX]Y�RYK�Y�\�[��\Έ�YJB��\��[ۈ[�Y�][
JB�ܙX]Y]]U[YHY�][
���
JB�\]Y]]U[YH\]Y]�B��[�[^[Y[�Y��[��YY�][
�ZY

JB��ڙX�Y��[��ڙX��ڙX��[][ۊ�Y[Έ��ڙX�YK�Y�\�[��\Έ�YJB�[[�[�X�[X[��X�[X[
L�B�ZY]]U[YB�Y]���[����\���[���X�ܙY�RY��[��X�ܙY�H\�\��[][ۊ�^[Y[��X�ܙY�H��Y[ΈܙX�ܙY�RYK�Y�\�[��\Έ�YJB�ܙX]Y]]U[YHY�][
���
JB�B����KKKKKKKKKH3�s�3��3������KKKKKKKKKB��[�[[��[�ܞR][HY��[��YY�][
�ZY

JB���H��[��[�\]YB�\�ܚ\[ۈ��[�[�]��[����3�3�s�3�3��s���3�3�s�3�s���p���3���s����]X[�]Sے[�X�[X[��X�[X[
L�HY�][

B�\]Y]]U[YH\]Y]�B��[�[[��[�ܞU�[��X�[ۈY��[��YY�][
�ZY

JB�[��[�ܞR][RY��[�[HX�[X[��X�[X[
L�H��
�3�3�s�s�s��s����8�$�3�s�s�3���s���X\�ۈ��[��[]Y�ڙX�Y��[��ܙX]Y�RY��[�ܙX]Y]]U[YHY�][
���
JB�B����KKKKKKKKKH3�3����s�sᳬH�3���s�3����s�sᳫ�s��KKKKKKKKKB��[�[�ڙX���[Y[�Y��[��YY�][
�ZY

JB��ڙX�Y��[��ڙX��ڙX��[][ۊ�Y[Έ��ڙX�YK�Y�\�[��\Έ�YJB��[���[����3���s�3���s�s���s�H�3���s�3�s���3�s���s�s����ܘY�U\���[����[��3�3�s����ؚ�X��ܘY�H
��X��\]X�JH8�%3�3��H�[�\�H3���3����\�YY�RY��[�ܙX]Y]]U[YHY�][
���
JB�B����KKKKKKKKKH�\��X�HKKKKKKKKKB��[�[�\��X�UX��]Y��[��YY�][
�ZY

JB��ڙX�Y��[��ڙX��ڙX��[][ۊ�Y[Έ��ڙX�YK�Y�\�[��\Έ�YJB�\��YH��[��]\���[��Y�][
��S��B�\��YۙY�Y��[��\��YۙY�\�\���[][ۊ��\��X�P\��YۙYȋ�Y[Έ�\��YۙY�YK�Y�\�[��\Έ�YJB��\���Y]]U[YOܙX]Y]]U[YHY�][
���
JB�\]Y]]U[YH\]Y]�B����KKKKKKKKKH]Y]��KKKKKKKKKB��[�[HX�ܕ\HT�T��RB��T�SB�B��[�[]Y]��Y��[��YY�][
�ZY

JB�X�ܕ\HX�ܕ\B�X�ܕ\�\�Y��[��X�ܕ\�\�\�\���[][ۊ�]Y]X�ܕ\�\���Y[Έ�X�ܕ\�\�YK�Y�\�[��\Έ�YJB�X�ܓX�[��[����3���ˈ��UQH�3�3�3�s�HX�ܕ\OPRB�X�[ۈ��[��Y�ܙH��ۏY�\���ۏ��\��H��[��[]Y�ڙX�Y��[���[]Y�\��Y\�Y��[��]]U[YHY�][
���
JB��[�^
ܙ[]Y�ڙX�YJB�[�^
ܙ[]Y�\��Y\�YJB�[�^
�]JB�B
